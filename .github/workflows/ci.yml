name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  notion-context-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: notion-context-sync/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: notion-context-sync
      - run: ./scripts/check-public-safety.sh
      - run: pnpm run typecheck
        working-directory: notion-context-sync
      - run: pnpm test
        working-directory: notion-context-sync

  notion-context-mcp-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: notion-context-mcp-worker/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: notion-context-mcp-worker
      - run: ./scripts/check-public-safety.sh
      - run: pnpm run typecheck
        working-directory: notion-context-mcp-worker
      - run: pnpm test
        working-directory: notion-context-mcp-worker
